# Generated by audit-gen --format=splunk-ta — DO NOT EDIT by hand.
# Regenerate with: audit-gen --format=splunk-ta --input <taxonomy.yaml> --output <dir>

[install]
state = enabled
# is_configured must be false for Splunkbase publishing — true claims
# the operator has already finished setup, which doesn't apply to a
# generated TA installed for the first time.
is_configured = false
build = 1

[ui]
is_visible = false
label = TA-axonops-audit

[launcher]
author = AxonOps
description = CIM Change-aligned Technology Add-on for audit events emitted by the github.com/axonops/audit library.
version = 0.1.0

# [package] id MUST match the uncompressed directory name; the
# generator derives PackageID from filepath.Base(outDir).
[package]
id = splunk-ta-axonops-audit

[id]
name = splunk-ta-axonops-audit
version = 0.1.0
