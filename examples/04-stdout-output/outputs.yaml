# Stdout output configuration — the simplest possible setup.
#
# Stdout is built into the core module. No blank import needed,
# no type-specific configuration block. Just declare type: stdout.

version: 1
app_name: "stdout-example"
host: "dev-machine"
outputs:
  console:
    type: stdout
